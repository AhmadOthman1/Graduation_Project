import 'package:flutter/material.dart';

class ShowJob extends StatelessWidget {
  const ShowJob({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("The Job"),),
    );
  }
}