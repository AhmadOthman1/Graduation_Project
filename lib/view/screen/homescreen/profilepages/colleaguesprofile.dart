import 'package:flutter/material.dart';

class ColleaguesProfile extends StatelessWidget {
  const ColleaguesProfile({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Obaida Aws"),),
    );
  }
}